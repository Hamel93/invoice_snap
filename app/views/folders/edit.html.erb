<% content_for :title, "Modifier le classeur" %>

<%= render "shared/screen_header", title: "Modifier le classeur", back: folder_path(@folder) %>

<div class="app-pad" style="margin-top:18px;">
  <%= render "form", folder: @folder %>
</div>
