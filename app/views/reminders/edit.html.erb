<% content_for :title, "Modifier le rappel" %>

<%= render "shared/screen_header", title: "Modifier le rappel", back: reminders_path %>

<div class="app-pad" style="margin-top:18px;">
  <%= render "form", reminder: @reminder, invoices: @invoices %>
</div>
